@page "/booksales"
@page "/authors/{AuthorId:int}/booksales"
@attribute [Authorize]

@code {
    [Parameter]
    public int? AuthorId { get; set; }
}

<ShowList Type="booksales" AuthorId="@AuthorId" />
